/**
 * Annotation Selection Types
 * Types and enums for annotation selection modes
 */

/**
 * Selection mode for annotation visibility manager
 */
export enum SelectionMode {
  SINGLE = 'single',
  MULTIPLE = 'multiple',
  NONE = 'none',
}

/**
 * Visibility action types
 */
export enum VisibilityAction {
  SHOW = 'show',
  HIDE = 'hide',
  TOGGLE = 'toggle',
}
