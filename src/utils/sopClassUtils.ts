/**
 * SOP Class Utilities
 *
 * This file contained utility functions for SOP class handling that were not being used.
 * All unused exports have been removed to improve bundle size and maintainability.
 *
 * Only the SOPClassConfig type re-export is kept for compatibility.
 */

// Re-export SOPClassConfig type for convenience (used in other parts of the codebase)
export type { SOPClassConfig } from '../services/sopClassHandler';
